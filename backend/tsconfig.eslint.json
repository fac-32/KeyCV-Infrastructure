{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "."  // Allow root-level files
  },
  "include": [
    "src/**/*",
    "*.ts",  // Include root-level .ts files
    "*.mts",
    "*.cts"
  ],
  "exclude": ["node_modules", "dist"]
}
